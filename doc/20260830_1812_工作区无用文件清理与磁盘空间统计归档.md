# 工作区无用文件与冗余缓存清理与磁盘空间统计报告

- **归档时间**：2026-08-30 18:12
- **责任模块**：工作区工程整理 / 临时文件清理 / 磁盘占用统计与产物审计

---

## 1. 清理与整理详情

### 1.1 已清理的无用与冗余项
1. **`genesis-doc/genesis-world-main/genesis-world-main` (220 MB)**：
   - 解压时产生的嵌套重复目录，已完全移除，保留最外层完整文档体系；
2. **`1111/test_demo_script` 编译产物与缓存 (75 MB)**：
   - 包含 ROS 2 colcon 历史构建生成的 `build/`, `install/`, `log/` 以及临时提取的 `docx_extract/` 目录；
3. **`output/` 目录临时中间帧与测试 OBJ (18 MB)**：
   - 清理了排查穿模时提取的 `inspect_frames/` 和 `inspect_mesh_frames/` 临时单帧图片；
   - 清理了早期单体测试导出的 `couch_mesh.obj`（正式网格已统一收敛至 `scene_mesh_cache/`）；
   - 清理了早期测试生成的 `test_planar_motion.gif`, `test_wide_cinematic.png`, `test_3d_topdown_view.png` 等临时验证文件；
4. **`render_check/` 历史临时渲染文件夹 (872 KB)**：
   - 初期调试相机视口遗留的临时测试截图；
5. **系统临时日志与备份**：
   - 根目录下 `drive_position.log`、`genesis_smoke/logs/*.log` 以及所有 `*.bak` 备份文件；
   - 全工作区 `__pycache__` 与 `*.pyc` 字节码缓存。

---

## 2. 清理后工作区磁盘占用统计

| 目录/文件 | 清理后占用大小 | 内容说明 |
| :--- | :--- | :--- |
| **`GRScenes/`** | 105 GB | GRScenes-100 室内三维场景数据集与高精 USD 资产库 |
| **`InternUtopia/`** | 667 MB | R2 机器人 CAD 几何 STL 网格与仿真环境核心库 |
| **`maniskill_r2/`** | 366 MB | 3D 仿真、PPO 策略权重、拓扑语义图、VLM 搜索模块与场景网格缓存 |
| **`robot_example1_EN_demo/`** | 346 MB | R2 实机 ROS 2 驱动与控制器参考工程 |
| **`genesis-doc/`** | 221 MB | Genesis 物理引擎官方参考文档（已去重） |
| **`output/`** | 28 MB | **核心交付产物**（包含所有基准评测报告、3D 真实网格导航 MP4 视频与 GIF 动图） |
| **`genesis_smoke/`** | 14 MB | Genesis 引擎初期冒烟验证脚本与工具链 |
| **`1111/`** | 11 MB | 轮式具身智能机器人教程文档 |
| **`.agents/`** | 808 KB | 代理技能配置与环境定义 |
| **`doc/`** | 88 KB | 阶段技术交接与里程碑归档文档 |
| **根目录代码与文档** | 32 KB | `交接文档.md`, `skills-lock.json` 等配置 |
| **合计** | **~106.6 GB** | **累计释放约 315 MB 冗余缓存与临时文件** |
