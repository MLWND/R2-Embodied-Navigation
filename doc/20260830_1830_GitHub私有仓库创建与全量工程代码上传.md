# GitHub 私密仓库创建与全量工程代码同步报告

- **归档时间**：2026-08-30 18:30
- **责任模块**：代码版本管理 / GitHub API 自动化集成 / 工程远端同步

---

## 1. 远端私有仓库创建信息

- **GitHub 账户**：`MLWND`
- **仓库名称**：[`R2-Embodied-Navigation`](https://github.com/MLWND/R2-Embodied-Navigation)
- **仓库属性**：**Private（私密仓库）**
- **主分支**：`main`
- **仓库链接**：`https://github.com/MLWND/R2-Embodied-Navigation.git`

---

## 2. 提交与同步内容清单

已将整理后的核心模块全部安全推送到远端私密仓库：

1. **`maniskill_r2/`**：
   - VLM 主动视觉搜索与 3D 反投影定位模块 (`vlm_active_search.py`)；
   - 室内空间语义拓扑连通图与路由规划器 (`scene_semantic_graph.py`)；
   - PPO 强化学习策略模型与 48 维 LiDAR APF 门洞避障控制器 (`ppo_nav_agent.py`, `grscene_vlm_nav.py`)；
   - 训练完成的 69 场景策略网络权重 (`models/ppo_r2_nav_69scenes_full.pt`)；
   - 3D 高精 CAD 真实网格渲染生成器与实时 Web 监控服务 (`render_real_3d_mesh_navigation.py`, `realtime_nav_streamer.py`)；
2. **`InternUtopia/`**：
   - R2 轮式拟人机器人 URDF 机构定义、传感器挂载配置与底层关节控制器；
3. **`robot_example1_EN_demo/`**：
   - R2 实机 ROS 2 驱动、Slave Executor 节点与 launch 配置；
4. **`doc/`**：
   - 包含所有里程碑交接文档与技术报告；
5. **`output/`**：
   - 包含所有基准评测结果 JSON、对比分布图表与高清展图；
6. **项目工程配置**：
   - `README.md`, `.gitignore`, `交接文档.md`。

---

## 3. 安全防护处理
- 已对本地 `git remote` 地址进行脱敏，移除了包含 Token 的明文连接串，确保后续环境安全。
